name: Déploiement CI/CD de l'application Node.js sur EC2

on:
  push:
    branches:
      - main # Déclencher ce workflow à chaque 'push' sur la branche principale

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    
    # 1. Récupération du code
    - name: 📦 Récupération du Code
      uses: actions/checkout@v4
      
    # 2. Configuration de l'environnement Node.js
    - name: ⚙️ Configuration de Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20' # Assurez-vous d'utiliser la bonne version
        
    # 3. Installation des dépendances et Build (Exécuté sur GitHub)
    - name: 🏗️ Installation et Construction du Projet
      run: |
        npm install
        npm run build 
        
    # --- Déploiement vers EC2 ---

    # 4. Transfert des Fichiers (SCP - Secure Copy)
    # Cette action copie tous les fichiers, y compris le dossier 'build', vers l'EC2.
    - name: 💾 Transfert des Fichiers (SCP)
      uses: appleboy/scp-action@master
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        source: "." # Transférer tout le contenu du répertoire courant GitHub
        target: "/home/${{ secrets.EC2_USER }}/myapp" # Cible sur l'instance EC2
        
    # 5. Redémarrage de l'Application (SSH - Remote Command)
    # Cette action exécute les commandes sur l'EC2 pour mettre à jour l'application.
    - name: 🔄 Redémarrage de l'Application (SSH)
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        script: |
          APP_DIR="/home/${{ secrets.EC2_USER }}/myapp" 
          cd $APP_DIR
          
          echo "Dossier actuel : $PWD"
          
          # Mise à jour et redémarrage de l'application via PM2
          # Utilise 'reload' pour un redémarrage doux si l'app existe, sinon 'start'
          pm2 reload my-node-app || pm2 start npm --name "my-node-app" -- start
          
          # Enregistrer l'état de PM2 pour qu'il redémarre après un reboot de l'EC2
          pm2 save